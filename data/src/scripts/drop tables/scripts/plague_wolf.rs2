[ai_queue3,plague_wolf] @plague_wolf_droptable;


[label,plague_wolf_droptable]
gosub(npc_death);
if (npc_findhero = false) {
    return;
}

// Default drop from config.
obj_add(npc_coord, npc_param(death_drop), 1, ^lootdrop_duration);

obj_add(npc_coord, plague_wolf_fur, 1, ^lootdrop_duration);